# Add webuser and disable shell
$ sudo adduser --home=/var/www/website.nl --shell=/bin/false website

# Compress a folder
$ tar -zcvf x.tar.gz path/

# Extract a folder or into a specified directory, 
$ tar -zxvf x.tar.gz
$ tar -zxvf x.tar.gz -C path/

# WP cli
$ wp search-replace foo bar --export=db.sql

# SSL
$ sudo certbot certonly --webroot -w /var/www/site.com/web -d site.com -d www.site.com
$ sudo certbot -> install + secure redirect